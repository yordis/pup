use anyhow::{bail, Result};

pub fn list() -> Result<()> {
    bail!("obs-pipelines commands are not yet implemented (API endpoints pending)")
}

pub fn get(_pipeline_id: &str) -> Result<()> {
    bail!("obs-pipelines get is not yet implemented (API endpoints pending)")
}
