use anyhow::{bail, Result};

pub fn list() -> Result<()> {
    bail!("scorecards commands are not yet implemented (API endpoints pending)")
}

pub fn get(_scorecard_id: &str) -> Result<()> {
    bail!("scorecards get is not yet implemented (API endpoints pending)")
}
