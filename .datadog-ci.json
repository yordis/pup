{
  "apiKey": "${DATADOG_API_KEY}",
  "appKey": "${DD_APP_KEY}",
  "site": "${DD_SITE}",
  "files": [
    "coverage.out"
  ],
  "coverage": {
    "format": "go-cover",
    "basePath": "."
  },
  "sast": {
    "enabled": true,
    "languages": ["go"],
    "rules": "recommended"
  }
}
